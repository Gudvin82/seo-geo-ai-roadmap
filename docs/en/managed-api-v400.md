# Managed API Boundary v4.0.0

The project remains self-hosted-first, but `v4.0.0` makes the managed boundary explicit.

Core managed-style resources:

- scanner URL audit
- scan job result
- task bundle
- graph snapshot
- executive dashboard

Use `/api/v1/settings/managed-api-boundary` for the machine-readable summary.
