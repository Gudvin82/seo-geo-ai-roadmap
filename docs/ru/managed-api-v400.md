# Managed API Boundary v4.0.0

Проект по-прежнему self-hosted-first, но в `v4.0.0` managed boundary уже явно оформлен.

Ключевые managed-style resources:

- scanner URL audit
- scan job result
- task bundle
- graph snapshot
- executive dashboard

Используйте `/api/v1/settings/managed-api-boundary` для machine-readable summary.
