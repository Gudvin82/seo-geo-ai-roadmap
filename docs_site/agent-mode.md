# Agent Mode

`v4.0.0` adds a real agent-mode layer with safe boundaries, task generation, alerts, and executive summaries.

Machine-readable paths:

- `GET /api/v1/agent-mode/contract`
- `GET /api/v1/agent-mode/overview`
- `POST /api/v1/agent-mode/runs`
